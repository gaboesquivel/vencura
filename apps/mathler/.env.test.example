# E2E test env — copy to .env.test (gitignored). Matches Playwright + Dynamic sandbox.
# Section order aligns with apps/mathler/.env.local.example

# -----------------------------------------------------------------------------
# Client (NEXT_PUBLIC_*)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_DYNAMIC_ENVIRONMENT_ID=your_sandbox_environment_id

# -----------------------------------------------------------------------------
# Runtime
# -----------------------------------------------------------------------------
# Matches `pnpm start` / Playwright (package.json uses `next start -p 3002`).
PORT=3002

# -----------------------------------------------------------------------------
# E2E / Playwright
# -----------------------------------------------------------------------------
PLAYWRIGHT_APP_URL=http://localhost:3002
# E2E_TEST_EMAIL=
# E2E_STATIC_OTP=

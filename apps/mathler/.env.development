# Development — committed, non-sensitive defaults only.
# Secrets: `.env.local` (see `.env.local.example`).

# -----------------------------------------------------------------------------
# Runtime
# -----------------------------------------------------------------------------
# Listen port: `package.json` uses `next dev -p 3002` (Next.js CLI). Do not rely on PORT here to bind the dev server.

# -----------------------------------------------------------------------------
# Optional integrations
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_SENTRY_ENVIRONMENT=development

# Copy to .env — see apps/mobile README. Placeholders only.
# Expo public vars (EXPO_PUBLIC_*) are exposed to the client bundle.

# -----------------------------------------------------------------------------
# Client (EXPO_PUBLIC_*)
# -----------------------------------------------------------------------------
# API URL reachable from simulator/device (port forward / LAN)
EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_DYNAMIC_ENVIRONMENT_ID=your_dynamic_environment_id

# Test environment configuration
# This file is committed and contains non-sensitive test configuration
# Sensitive values (DYNAMIC_ENVIRONMENT_ID, DYNAMIC_API_TOKEN, ENCRYPTION_KEY) come from .env file

# Use local Anvil blockchain for tests (default)
USE_LOCAL_BLOCKCHAIN=true

# Point Arbitrum Sepolia to local Anvil for local blockchain testing
# Dynamic SDK doesn't support localhost chains, so we use Arbitrum Sepolia chain ID
# while RPC URLs point to localhost:8545
RPC_URL_421614=http://localhost:8545

# Development Environment Configuration
# This file is committed - contains non-sensitive configuration only

# Port (optional, defaults to 3077)
PORT=3077

# Local blockchain configuration
# Use local Anvil blockchain (default: true for development)
USE_LOCAL_BLOCKCHAIN=true

# Optional: Per-chain RPC URL overrides
# Format: RPC_URL_<CHAIN_ID>=... or RPC_URL_<DYNAMIC_NETWORK_ID>=...
# For local testing with Anvil, point test chains to localhost:
RPC_URL_421614=http://localhost:8545
RPC_URL_84532=http://localhost:8545

# Backward compatibility: ARBITRUM_SEPOLIA_RPC_URL still works
# ARBITRUM_SEPOLIA_RPC_URL=http://localhost:8545

# Optional: Swagger UI (default: false for security)
# ENABLE_SWAGGER_UI=false

# Staging Environment Configuration
# This file is committed - contains non-sensitive configuration only

# Port (optional, defaults to 3077)
PORT=3077

# Local blockchain configuration
# Use testnet (not local blockchain)
USE_LOCAL_BLOCKCHAIN=false

# Optional: Per-chain RPC URL overrides for testnets
# Format: RPC_URL_<CHAIN_ID>=... or RPC_URL_<DYNAMIC_NETWORK_ID>=...
# RPC_URL_421614=https://arbitrum-sepolia.infura.io/v3/your_key
# RPC_URL_84532=https://base-sepolia.infura.io/v3/your_key
# SOLANA_RPC_URL=https://api.devnet.solana.com

# Optional: Swagger UI (default: false for security)
# ENABLE_SWAGGER_UI=false

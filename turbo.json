{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", "bin/**", "src/**/*.ts"],
      "env": [
        "DYNAMIC_ENVIRONMENT_ID",
        "DYNAMIC_API_TOKEN",
        "SEPOLIA_RPC_URL",
        "ENCRYPTION_KEY",
        "RPC_URL_*",
        "ARBITRUM_SEPOLIA_RPC_URL",
        "SOLANA_RPC_URL"
      ]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
